<h1>All Products</h1>

<% @products.each do |f| %>

  <p><%= f.name %></p>

  <p><%= f.description %></p>

  <p>$<%= f.formatted_price %></p>

  <%= link_to 'Edit', edit_product_path(f) %> | <%= link_to 'Delete', product_path(f), method: :delete %>
<br>
<br>
<% end %>

<br>
<br>
<br>
<%= link_to 'Add New Product', new_product_path %>
<br>
<%= link_to 'Login', new_session_path %>
